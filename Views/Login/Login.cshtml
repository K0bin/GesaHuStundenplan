@{
    var loginProviders = Context.Authentication.GetAuthenticationSchemes().ToList();
    <form asp-action="ExternalLogin" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post" class="form-horizontal" role="form">
        <div>
            <p>
                <button type="submit" class="btn btn-default" name="provider" value="Google" title="Google">Google</button>
            </p>
        </div>
    </form>
}
